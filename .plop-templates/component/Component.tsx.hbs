export interface {{pascalCase name}}Props extends React.PropsWithChildren {}

export const {{pascalCase name}}: React.FC<{{pascalCase name}}Props > = ({}) => {
    return (
        <div>{{pascalCase name}}</div>
    );
};